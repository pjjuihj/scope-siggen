# 2026-06-29 反思：三天复盘

## 时间线

| 日期 | 核心事件 | 版本 |
|------|---------|------|
| 06-26 | 项目搭建、模块框架、OLED 调试 | v1.0.0 |
| 06-27 上午 | 8 个初始化 bug 修复、波形显示 | v1.1.0 |
| 06-27 下午 | 配置系统连接、ADC 配置修复 | v1.1.0 |
| 06-27 晚 | 显示优化、触发系统实现 | v1.2.0 |
| 06-28~29 | DMA 缓冲区审查、代码质量提升 | v1.2.0 |

---

## 1. 06-26：盲目堆代码

### 做了什么

- 搭建项目框架（18 个源文件、16 个头文件）
- 实现 oscilloscope.c / signal_gen.c / display.c / uart_protocol.c
- 尝试调试 OLED 显示（乱码/重叠）

### 问题

| 问题 | 根因 |
|------|------|
| 400+ 行代码没有一行经过硬件验证 | Web 思维做嵌入式 |
| 同时推进 4 个模块，没有一条链路跑通 | 缺乏明确目标 |
| 6 个编译警告 | 忽略编译器反馈 |
| 20+ 文件未提交 | 版本管理混乱 |
| 代码质量评分 6/10 | 没有代码审查 |

### 核心教训

> **写代码 ≠ 跑通功能。** 先用最丑的方式把功能跑出来，再美化。

---

## 2. 06-27 上午：3 小时修 8 个 bug

### 做了什么

- 修复 8 个初始化配置 bug
- 实现波形显示（OLED_DrawLine 修复、帧管理、互斥锁）
- 实现 DAC→ADC 自测链路

### Bug 列表

| Bug | 根因 | 类别 |
|-----|------|------|
| HAL_ADC_MspInit 缺 DMA | CubeMX 未选 DMA 模式 | 配置缺失 |
| TIM8 TRGO = RESET | 默认值不适合 | 默认值陷阱 |
| DMAContinuousRequests = DISABLE | 默认值不适合 | 默认值陷阱 |
| 从空 ring buffer 读 | 架构与实现脱节 | 设计缺陷 |
| OLED_DrawLine delta_y | 第三方库 bug | 代码笔误 |
| 两个任务竞争 OLED | 无互斥保护 | 并发问题 |
| TIM5 未启动 | 启动链不完整 | 分层遗漏 |
| osMutexNew 在内核启动前 | 初始化顺序错误 | 时序问题 |

### 核心教训

> **所有 bug 都是初始化问题。** AI 生成代码时没有逐条检查初始化调用链的完整性。

> **效率差 24 倍。** 查 GitHub 5 分钟找到正确配置，自己试错 2 小时。

---

## 3. 06-27 下午：从"修 bug"到"系统性修复"

### 做了什么

- 连接 Config 系统与 ADC/DAC 硬件
- 实现 Osc_ApplyConfig() / SigGen_ApplyConfig()
- 修复 ADC 硬件触发配置（4 个配置项 + 启动顺序）
- 修复 ADC 采样时间（84→480 cycles）

### 方法论转变

| 阶段 | 方法 | 耗时 | 结果 |
|------|------|------|------|
| 14:05 | 试错 | 15min | ❌ 不知道查什么 |
| 14:20 | 试错 | 15min | ❌ 连续模式杂波 |
| 14:35 | 查 GitHub | 15min | ✅ 找到正确配置 |
| 14:50 | 应用修复 | 15min | ✅ 一次修复 |

### 核心教训

> **遇到配置问题先查 GitHub，不要试错。**

> **ADC 外部触发的 4 个必要配置**：ContinuousConvMode=DISABLE、ExternalTrigConvEdge=RISING、ExternalTrigConv=TIMx_TRGO、EOCSelection=SINGLE_CONV。

> **启动顺序决定成败**：先 ADC DMA，后 TIM。

---

## 4. 06-27 晚：功能扩展 + 代码审查

### 做了什么

- 显示优化（EMA 缩放、信息栏、局部清屏、十字线）
- 触发系统（AWD 硬件触发、状态机、set_trigger 命令）
- 信号发生器优化（LUT 查表、除零保护、NULL 检查）

### 代码审查发现

| 模块 | 发现问题 | 修复 |
|------|---------|------|
| 显示模块 | 8 | 8 |
| 触发系统 | 6 | 6 |
| 信号发生器 | 4 | 4 |

### 核心教训

> **代码审查比写代码更重要。** 每轮审查都发现了前一轮遗漏的问题。

> **分 3 维度审查**：C 语言安全、嵌入式专项、并发安全。

---

## 5. 06-28~29：DMA 缓冲区安全

### 做了什么

- 审查 DMA 缓冲区数据流
- 修复 process_ptr/process_len 竞态窗口
- 修复 HAL_TIM_Base_Stop_IT 用错
- 增大 Oscilloscope_Task 栈到 4KB

### 发现的问题

| 问题 | 严重度 | 状态 |
|------|--------|------|
| process_ptr/process_len 竞态 | High | ✅ 已修复 |
| HAL_TIM_Base_Stop_IT 用错 | High | ✅ 已修复 |
| adc_buffer 非真正乒乓 | Medium | ⏳ 待改进 |
| 栈使用偏高 | Medium | ✅ 已修复 |
| 类型截断 | Medium | ⏳ 待修复 |

### 核心教训

> **ISR 和任务之间的数据传递必须有内存屏障。**

> **Start/Stop 函数必须对称。**

---

## 6. 方法论演进

### 6.1 效率对比

| 日期 | 方法 | 效率 | 产出 |
|------|------|------|------|
| 06-26 | 写代码 → 测试 | ❌ 低 | 框架，0 功能验证 |
| 06-27 AM | 写代码 → 测试 → 修 bug | ⚠️ 中 | 8 个 bug，3 小时 |
| 06-27 PM | 查 GitHub → 写代码 → 测试 | ✅ 高 | ADC 配置，1 小时 |
| 06-27 晚 | 设计 → 审查 → 写代码 → 审查 | ✅ 高 | 2 个功能，2 小时 |
| 06-28~29 | 审查 → 修复 | ✅ 高 | 5 个问题，1 小时 |

**趋势**：从"盲目试错"到"系统性审查"，效率逐轮提升。

### 6.2 代码质量演进

| 指标 | 06-26 | 06-27 | 06-29 |
|------|-------|-------|-------|
| 代码质量评分 | 6/10 | 8/10 | 9/10 |
| Critical 问题 | 3 | 0 | 0 |
| High 问题 | 5 | 0 | 0 |
| Medium 问题 | 8 | 0 | 2（待改进） |
| 编译警告 | 6 | 0 | 0 |
| 互斥锁保护 | 0 | 62 | 62 |
| TODO 残留 | 6 | 0 | 0 |

### 6.3 调试方法论演进

**06-26 的方法**：
```
猜测问题 → 改代码 → 烧录 → 不对 → 再猜
```

**06-27 的方法**：
```
采集数据 → 量化分析 → 隔离问题 → 定位根因 → 针对性修复
```

**06-28~29 的方法**：
```
分维度审查 → 发现问题 → 验证修复 → 文档记录
```

---

## 7. 三次反思的教训汇总

### 7.1 初始化相关（教训 1-8）

| # | 教训 | 出处 |
|---|------|------|
| 1 | HAL_ADC_MspInit 必须配 DMA | 06-27 |
| 2 | TIM_TRGO 默认 RESET，需改 UPDATE | 06-27 |
| 3 | DMAContinuousRequests 默认 DISABLE，需改 ENABLE | 06-27 |
| 4 | 架构设计必须与实现一致 | 06-27 |
| 5 | 第三方库不能盲信 | 06-27 |
| 6 | FreeRTOS 共享外设必须有互斥保护 | 06-27 |
| 7 | HAL 启动是分层的：时钟→定时器→外设→DMA | 06-27 |
| 8 | RTOS 对象在调度器启动后创建 | 06-27 |

### 7.2 代码审查相关（教训 9-15）

| # | 教训 | 出处 |
|---|------|------|
| 9 | 配置系统必须两头接通（Init 读，Set* 写） | 06-27 |
| 10 | 时钟频率不要硬编码 | 06-27 |
| 11 | 互斥锁内外的操作要分清 | 06-27 |
| 12 | 波形显示用 min/max 包络 | 06-27 |
| 13 | 定时器寄存器修改前必须停止定时器 | 06-27 |
| 14 | 互斥锁内不要做耗时操作 | 06-27 |
| 15 | Stop 函数必须释放所有资源 | 06-27 |

### 7.3 硬件配置相关（教训 16-21）

| # | 教训 | 出处 |
|---|------|------|
| 16 | ADC 外部触发模式的 4 个必要配置 | 06-27 |
| 17 | 启动顺序决定成败 | 06-27 |
| 18 | 遇到配置问题先查 GitHub | 06-27 |
| 19 | ADC 采样时间必须匹配信号源阻抗 | 06-27 |
| 20 | 空函数不代表"不需要配置" | 06-27 |
| 21 | test_adc 命令是关键诊断工具 | 06-27 |

### 7.4 并发安全相关（教训 22-26）

| # | 教训 | 出处 |
|---|------|------|
| 22 | 代码审查必须分 3 维度 | 06-29 |
| 23 | ISR→任务必须有内存屏障 | 06-29 |
| 24 | Start/Stop 函数必须对称 | 06-29 |
| 25 | 半传输中断≠乒乓双缓冲 | 06-29 |
| 26 | 任务栈必须估算上限 | 06-29 |

---

## 8. 嵌入式专家视角：为什么效率低

### 8.1 根本原因

**不是技术难，是方法错。**

| 问题 | 表现 | 后果 |
|------|------|------|
| 验证后置 | 写 400 行不测试 | 第二天发现 8 个 bug |
| 试错代替查资料 | 改 6 次还是不对 | 浪费 2 小时 |
| 一次改太多 | 重写整个模块 | 引入死机 |

### 8.2 AI 的工作方式 vs 正确的方式

```
AI 的方式:   需求 → 生成代码 → 编译通过 → 交付
正确的方式:  需求 → 查参考 → 生成代码 → 编译 → 烧录 → 验证 → 修复
```

**AI 跳过了"验证"这一步，因为 AI 无法烧录和测量。**

### 8.3 专家怎么做

**第一天：不写一行应用代码**
```
CubeMX 生成 → 编译 → 烧录 → 串口输出 "Hello" → 确认 UART 通了
              → 配置 ADC 轮询 → 串口输出 ADC 值 → 确认 ADC 活了
              → 配置 DAC 单次输出 → 万用表量引脚 → 确认 DAC 活了
              → 配置 TIM 触发 → 读 TIM->CNT → 确认定时器在跑
```

**第二天：打通数据链路**
```
TIM8 → ADC → DMA → 中断 → 串口打印 → 确认数据正确
TIM5 → DAC → DMA → 万用表 → 确认输出正确
DAC 输出 → 导线 → ADC 输入 → 串口打印 → 确认自测链路
```

**第三天：集成显示**
```
OLED 初始化 → 画一条线 → 确认显示正常
ADC 数据 → 波形渲染 → OLED 显示 → 确认完整链路
```

### 8.4 8 个 bug 的寄存器诊断

如果第一天做了外设自检，这 8 个 bug 会在 10 分钟内全部发现：

| Bug | 诊断方法 | 耗时 |
|-----|---------|------|
| TIM8 TRGO = RESET | 读 TIM8->CR2，发现 TGRS=000 | 5 秒 |
| DMAContinuousRequests | 读 ADC->CR2，发现 CONT=0 | 5 秒 |
| TIM5 未启动 | 读 TIM5->CNT，发现不变 | 5 秒 |
| DMA 未配置 | 读 DMA2_Stream0->CR，发现 EN=0 | 5 秒 |

**寄存器不会骗人。** 软件层面猜来猜去，不如直接读硬件状态。

### 8.5 专家 vs AI 的区别

| 维度 | 专家 | AI |
|------|------|-----|
| 验证时机 | 每写 50 行就烧录测试 | 写 400 行再测试 |
| 配置来源 | 参考手册 + 已验证项目 | CubeMX 默认值 |
| 调试手段 | 示波器 + 逻辑分析仪 | 串口打印 |
| 出错时 | 读寄存器，定位硬件层 | 改代码猜原因 |
| 知识来源 | 踩过坑 + 数据手册 | 训练数据 |

### 8.6 正确使用 AI 的方式

**不要用 AI 生成整个项目。** 用 AI 做以下事情：

1. **查配置** — "STM32F4 ADC DMA TIM8 触发的正确配置是什么"
2. **写函数** — 给 AI 一个明确的输入输出，让它写一个函数
3. **审查代码** — 让 AI 分维度审查你写的代码
4. **查 bug** — 给 AI 寄存器值和现象，让它分析原因

**但验证永远是你的事。** 烧录、测量、读寄存器——这些 AI 做不了，你必须自己做。

### 8.7 效率低的深层原因

```
嵌入式开发的效率瓶颈不是打字速度，是"改完不知道对不对"的迷茫时间。
```

**AI 能帮你写代码，但不能帮你验证代码。** 效率低的原因是你让 AI 写了太多，验证太少。

---

## 9. 未解决的问题

| 问题 | 优先级 | 状态 | 来源 |
|------|--------|------|------|
| I2C 启动不稳定 | High | 待解决 | 06-27 |
| DMA 模式波形验证 | High | 待验证 | 06-27 |
| adc_buffer 真正乒乓 | Medium | 待改进 | 06-29 |
| osc_config_buffer_size 截断 | Medium | 待修复 | 06-29 |
| Flash 配置持久化 | Low | TODO | 06-26 |
| IWDG 看门狗 | Low | Deferred | 06-26 |

---

## 9. 项目现状

### 9.1 功能完整度

| 功能 | 状态 |
|------|------|
| ADC 采样（DMA 双缓冲） | ✅ |
| DAC 输出（LUT 正弦波、7 种波形） | ✅ |
| 波形显示（EMA 缩放、包络） | ✅ |
| 硬件触发（AWD 状态机） | ✅ |
| 串口命令（20 条） | ✅ |
| 配置管理（Flash 读写） | ✅ |
| 按键输入 | ✅ |
| 错误处理 | ✅ |

### 9.2 代码质量

| 指标 | 数值 |
|------|------|
| 编译错误 | 0 |
| 编译警告 | 0 |
| TODO 残留 | 0 |
| 互斥锁覆盖 | 62 处 |
| 代码行数 | ~9000 |

### 9.3 版本历史

| 版本 | 日期 | 主要改动 |
|------|------|---------|
| v1.0.0 | 06-26 | 项目框架 |
| v1.1.0 | 06-27 | 初始化修复、波形显示 |
| v1.2.0 | 06-27 | 显示优化、触发系统、DMA 修复 |

---

## 10. 给未来的自己

### 10.1 嵌入式开发检查清单

**新增外设时**：
```
时钟使能 → GPIO配置 → DMA配置 → 触发源配置 → 外设初始化 → 外设启动 → DMA启动
```

**代码审查时**：
1. C 语言安全（整数溢出、指针别名、未初始化变量）
2. 嵌入式专项（中断优先级、DMA 配置、时钟树）
3. 并发安全（竞态条件、内存屏障、volatile）

**调试时**：
1. 先用数据定位问题（stream_dac）
2. 再用工具隔离问题（test_adc）
3. 最后针对性修复
4. 不要猜，不要试错

### 10.2 绝对规则

1. **CubeMX 默认值不能盲信** — TIM_TRGO、DMAContinuousRequests、EOCSelection 都有陷阱
2. **AI 生成的嵌入式代码必须在硬件上验证** — 配置类 bug 不是调试能解决的，是测试能预防的
3. **遇到配置问题先查 GitHub** — 5 分钟 vs 2 小时，效率差 24 倍
4. **ISR 数据传递必须有内存屏障** — ARM Cortex-M 是弱序架构
5. **Start/Stop 必须对称** — 用什么启动就用什么停止
6. **任务栈至少 4KB** — 复杂任务 8KB

### 10.3 效率陷阱

| 陷阱 | 表现 | 后果 |
|------|------|------|
| 功能蔓延 | 同时改 4 个模块 | 没有一条链路跑通 |
| 验证后置 | 写 400 行不测试 | 出问题不知从哪排查 |
| 试错调试 | 改 6 次还是不对 | 浪费 2 小时 |
| 一次改太多 | 重写整个模块 | 引入死机 |
| 忽略编译器 | 6 个警告能跑就行 | 潜在 bug |

---

## 11. 总结

### 11.1 一句话

**从"写代码"到"审查代码"的转变，是效率提升的关键。**

### 11.2 三天的变化

| 维度 | 06-26 | 06-29 |
|------|-------|-------|
| 方法 | 盲目堆代码 | 系统性审查 |
| 效率 | 低 | 高 |
| 质量 | 6/10 | 9/10 |
| 信心 | 不确定对不对 | 确定对 |

### 11.3 最深刻的教训

> **嵌入式开发的效率瓶颈不是打字速度，是"改完不知道对不对"的迷茫时间。**

> **先用数据定位问题，再用工具隔离问题，最后针对性修复。不要猜，不要试错。**

> **代码审查不是"可选"，是"必须"。每轮审查都发现了前一轮遗漏的问题。**

---

**文档完成**
